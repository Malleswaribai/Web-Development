/**
 * git clone: to copy the folder from the github
 * git status: which give the status of how many files under commit, and staged 
 * git log: it gives all the commits which are performed on this repository
 * git add fileName: it makes our file under staged stage means it takes to staged stage from modified stage
 * git restore --staged fileName: it takes back from staged stage to modified stage
 * git commit -m "message"
 * 
 */